

#include <string>

class Nfa
{
public:
         Nfa     (const char* search);
    bool Matches (const char * line);
    
private:
};

Nfa::Nfa(const char* search)
{
    
}
bool Nfa::Matches(const char * line)
{
    return true;
}